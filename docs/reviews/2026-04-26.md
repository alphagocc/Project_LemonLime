# Code Review: 全代码库审计 — 2026-04-26

> **本文件取代 `docs/reviews/` 目录下所有早期 review。** 早期文件已在头部加上「过时」横幅，请勿再作为修复依据。

**审计范围**：`src/` 全部源码（base / core / component / 顶层 UI），约 50+ 个 .cpp，~30k 行
**审计人**：Claude Opus 4.7（Antigravity AI）
**基准 commit**：`a6f010d`（"fix: address bugs found in code review"）之后的工作树
**方法**：分模块并行派发审查代理，逐文件读取，重点查内存安全 / 数据竞争 / 误判 / XSS / 跨平台一致性

---

## 本轮已修复（commit `a6f010d`）

为避免重复列入未决项，先列出本次提交已修的 9 处：

| 位置 | 修复 |
| --- | --- |
| `src/base/settings.cpp:536` | 设置键拼写 `DefaultOuputFileExtension` → `DefaultOutputFileExtension` |
| `src/base/settings.h:145` | 移除未实现的 `temporaryPath()` 声明 |
| `src/base/LemonBaseApplication.cpp:25` | `parseCommandLine` 返回值取反，正常启动不再误打错误日志 |
| `src/base/LemonBaseApplication.cpp:51` | 释放局部 `Settings*` |
| `src/base/LemonLog.hpp:50-57` | `tempBuffer` / `tempStream` 移入 `log_concat` 局部，消除多线程数据竞争 |
| `src/base/LemonTranslator.cpp:55-60` | `QTranslator::load` 失败时直接返回，不再 install 空翻译器 |
| `src/core/processrunner_win.cpp:42` | `uniform_int_distribution` 上界由 `length()` 改为 `length()-1` |
| `src/generalsettings.cpp:131-134` | `checkValid()` 在 `rejudgeTimes` 为空时实际 `return false` |
| `src/lemon.cpp:972-986` | `changeContestName` 在 `!curContest` 时 `return`，避免空指针解引用 |

另在 [src/core/judgingthread.cpp:101-195](../../src/core/judgingthread.cpp#L101-L195) 的 `BufferedStreamReader` 上补充了注释（说明 32 字符是分块大小，不是行截断）。

---

## 撤回的早期结论

> 之前 audit 中的 **「`nextUntilNewLine`/`nextUntilSpace` 32 字符截断 → 误判 AC」** 错误。重读后确认：调用方在 `while (true)` 循环里反复调用直到 `eof()`，长行会被分块全部消费。**该条不再是 bug。**

> 之前 audit 中的 **「`JudgingDialog::setContest` 重复 connect」** 在当前调用图下也站不住脚：所有调用点（[detaildialog.cpp:265-267](../../src/detaildialog.cpp#L265-L267)、[resultviewer.cpp:258/268/298/328/361](../../src/resultviewer.cpp#L258)）都是 `new JudgingDialog(...)` 之后立即调用一次 `setContest`，不存在二次调用。**降级为「潜在风险」**：若将来引入 dialog 复用或重新选 contest，必须在新 connect 前 `disconnect`。

---

## 仍未修复的问题（按严重度分组）

### Critical

| 位置 | 描述 | 建议 |
| --- | --- | --- |
| [src/lemon.cpp:677-690](../../src/lemon.cpp#L677-L690) | `loadContest` 从文件读 `int len` 后直接 `new char[len]`，无任何边界校验。损坏 / 恶意 .cdf 触发堆破坏 | 改 `qint64`，对照 `file.size()` 校验 |
| [src/opencontestwidget.cpp:71](../../src/opencontestwidget.cpp#L71)、[:135](../../src/opencontestwidget.cpp#L135) | 同上模式 | 同上 |
| [src/exttestcasetable.cpp:233-244](../../src/exttestcasetable.cpp#L233-L244) | `haveSub` 为空时 `first()`/`last()` UB | 复用上方 `!haveSub.empty()` 守卫 |
| [src/core/judgingcontroller.cpp:47](../../src/core/judgingcontroller.cpp#L47) | 在 `taskJudger` 信号槽里 `delete taskJudger`，队列中的后续信号 use-after-free | 改 `taskJudger->deleteLater()` |
| [src/core/taskjudger.cpp:543](../../src/core/taskjudger.cpp#L543) + [taskjudger.h:66](../../src/core/taskjudger.h#L66) | `bool isJudging` 跨线程裸读写 | 改 `std::atomic<bool>` |
| [src/core/taskjudger.cpp:404-405](../../src/core/taskjudger.cpp#L404-L405) | 把状态码 `overallStatus[i]` 当分数 push 进 `score[i]`，污染 `min`/`getTaskScore` | 分开两个容器 |
| [src/core/taskjudger.cpp:416-420](../../src/core/taskjudger.cpp#L416-L420) | 跳过测点时只 emit 一个 `taskSkipped`，剩余测点 UI 不更新 | 在 break 前对剩余每个测点 emit |
| [src/core/taskjudger.cpp:243-310](../../src/core/taskjudger.cpp#L243-L310) | `CommunicationExec` 多文件循环编译，先失败的状态被后续成功覆盖 | 失败时 `break` 或保留 worst |
| [src/core/processrunner_unix.cpp:30-31, 197-203](../../src/core/processrunner_unix.cpp#L30-L31)、[processrunner_win.cpp:307-311](../../src/core/processrunner_win.cpp#L307-L311) | `stopFlag` 提前返回时 `res.result` 仍是 ctor 默认 `CorrectAnswer`，被中止运行报「0 分 AC」 | 在 stop 路径设 `res.result = Skipped` 或类似 |
| [src/core/judgingthread.cpp:761-769](../../src/core/judgingthread.cpp#L761-L769) | rejudge 时间窗算式 `1000 * extraTimeRatio` ≈ 100ms，明显与意图不符 | 应为 `timeLimit + 1000` 之类 |
| [src/base/LemonLog.hpp:62](../../src/base/LemonLog.hpp#L62) | `inline std::shared_ptr<spdlog::logger> logger` 默认初始化为 nullptr，第一次 `logger->...` 即崩 | 在 log_concat 顶部加 `if (!logger) return;` |

### High

#### 资源 / 句柄
- [src/core/processrunner_unix.cpp:36, 121](../../src/core/processrunner_unix.cpp#L36) — watcher 二进制每次复制后从不删除
- [src/core/processrunner_win.cpp:213-228](../../src/core/processrunner_win.cpp#L213-L228) — `CreateFileW` 不验 `INVALID_HANDLE_VALUE`，后续 `CloseHandle` 关错句柄
- [src/lemon.cpp:60](../../src/lemon.cpp#L60) — `signalMapper`、`TaskMenu` 无父对象，析构里只 delete 后者
- [src/optionsdialog.cpp:20-23](../../src/optionsdialog.cpp#L20-L23)、[exttestcasemodifierdialog.cpp:27](../../src/exttestcasemodifierdialog.cpp#L27)、[summarytree.cpp:323-342](../../src/summarytree.cpp#L323-L342)、[addcompilerwizard.cpp:425-582](../../src/addcompilerwizard.cpp#L425-L582) — 多处 `new` 路径未释放

#### Settings / 配置
- [src/base/settings.cpp:143, 268, 272](../../src/base/settings.cpp#L143) — `colorThemeList[index]` 无边界检查；`setColorTheme` 不 delete 旧 ColorTheme（泄漏）
- [src/base/settings.cpp:646](../../src/base/settings.cpp#L646) — `diffPath` 用 `QDir::currentPath()`，从其它目录启动 diff 失败；改 `QCoreApplication::applicationDirPath()`
- [src/base/LemonConfig.hpp:42-64](../../src/base/LemonConfig.hpp#L42-L64) — 多个 read/write/load/save 只声明无定义
- [src/base/LemonConfig.cpp:36-46](../../src/base/LemonConfig.cpp#L36-L46) — 解析失败路径泄漏已 append 的 Compiler

#### UI
- [src/compilersettings.cpp:157-168](../../src/compilersettings.cpp#L157-L168) — 删除编译器顺序错，槽里拿到错位/已删 `Compiler*`
- [src/welcomedialog.cpp:42-44](../../src/welcomedialog.cpp#L42-L44)、[opencontestdialog.cpp:41-43](../../src/opencontestdialog.cpp#L41-L43) — 行号 -1 时 `at(-1)` 风险
- [src/visualmainsettings.cpp:53-78](../../src/visualmainsettings.cpp#L53-L78) — `whenThemeEdit/Add` 视 `setColorTheme` 所有权语义可能泄漏 ColorTheme

#### 安全 / 编码
- [src/component/exportutil/exportutil.cpp:218-407, 574-679](../../src/component/exportutil/exportutil.cpp#L218-L407) — `QTextStream` 未设 utf-8，HTML meta 与实际写入不一致，Windows 中文乱码
- [src/detaildialog.cpp:139-221](../../src/detaildialog.cpp#L139-L221) 与 [exportutil.cpp:148, 155-158](../../src/component/exportutil/exportutil.cpp#L148) — 任务标题 / 选手名 / 文件名 / 编译信息直接拼入 HTML，**未做 HTML 转义 → 存储型 XSS**
- [src/component/exportutil/exportutil.cpp:723-753](../../src/component/exportutil/exportutil.cpp#L723-L753) — CSV 不转义 `"` 与换行，无 BOM，Excel 中文 / 含逗号项错位

#### 判题核心
- [src/core/processrunner_unix.cpp:198-208](../../src/core/processrunner_unix.cpp#L198-L208) — `terminate()` + `waitForFinished(-1)` 子进程不响应 SIGTERM 即挂死
- [src/core/processrunner_win.cpp:347-354](../../src/core/processrunner_win.cpp#L347-L354) — `GetProcessTimes` 返回值不查；多小时运行 `int * 60*60*1000` 溢出
- [src/core/processrunner_unix.cpp:235-266](../../src/core/processrunner_unix.cpp#L235-L266) — 不看 `exitStatus()`；bwrap 段错误退 0 当 AC
- [src/core/judgingthread.cpp:518, 622-643](../../src/core/judgingthread.cpp#L518) — SPJ 分数 `val * fullScore` int 溢出
- [src/core/contest.cpp:234-237](../../src/core/contest.cpp#L234-L237) — `stopJudgingSlot` 不查 `controller` 是否为 null

### Medium

- [src/lemon.cpp:166-184](../../src/lemon.cpp#L166-L184) — 文件监视器递归无防抖，单文件改动重建整棵树
- [src/lemon.cpp:309-512](../../src/lemon.cpp#L309-L512) — `cleanupButtonClicked` UI 线程长 IO，`setCancelButton(nullptr)` 禁掉取消
- [src/judgingdialog.cpp:29-31](../../src/judgingdialog.cpp#L29-L31) — 析构里 `delete cursor` 在 `delete ui` 之后；cursor 引用 `ui->logViewer->document()` → use-after-free（**修法：交换顺序**）
- [src/judgingdialog.cpp:43-56](../../src/judgingdialog.cpp#L43-L56) — `setContest` 不 disconnect（潜在）；当前调用路径未触发，但建议预防性加 `disconnect`
- [src/resultviewer.cpp:386-419](../../src/resultviewer.cpp#L386-L419) — `setCheckBox` 接管所有权后还 `delete checkBox` → 双重释放
- [src/testcaseeditwidget.cpp:133-141](../../src/testcaseeditwidget.cpp#L133-L141) — `for (i = 0; i <= range.rowCount(); i++)` 多删一次
- [src/core/judgingthread.cpp:78](../../src/core/judgingthread.cpp#L78) — 成员 `result` 未初始化
- [src/core/judgingthread.cpp:493-495, 600-603](../../src/core/judgingthread.cpp#L493-L495) — SPJ 等待循环里 `stopJudging` 直接 return，不设 `result/score/message`
- [src/core/taskjudger.cpp:152-173](../../src/core/taskjudger.cpp#L152-L173) — 所有同名 env 一律按 `;`/`:` 拼，腐蚀 `LANG` 等非路径变量
- [src/core/contest.cpp:199-212](../../src/core/contest.cpp#L199-L212) — `judge()` 用嵌套 `QEventLoop` 阻塞，stop 路径若不 emit `judgeFinished` 则永久挂起、controller 泄漏
- [src/core/processrunner_win.cpp:316-322](../../src/core/processrunner_win.cpp#L316-L322) — Win/Unix 对 watcher 超时分类不一致（Win=TLE，Unix=CannotStartProgram）
- [src/core/contestant.cpp:21-39](../../src/core/contestant.cpp#L21-L39) — 所有 getter 无边界检查
- [src/base/compiler.cpp:125-155](../../src/base/compiler.cpp#L125-L155) — `Compiler::read` 声明返回 -1 表错误，但 `READ_JSON` 全部丢弃返回值，永远返回 0
- [src/base/settings.cpp:371-415](../../src/base/settings.cpp#L371-L415) — `copyFrom` 漏拷 `recentContest`、`diffPath`（注释 `// 草` 为佐证）
- [src/component/exportutil/exportutil.cpp:430](../../src/component/exportutil/exportutil.cpp#L430)、[:671](../../src/component/exportutil/exportutil.cpp#L671) — `<a>` 写成 `<a>` 而非 `</a>`，HTML 标签嵌套错误
- [src/exttestcasemodifier.cpp:298-312](../../src/exttestcasemodifier.cpp#L298-L312) — 对 `TestCase` 值复制；若继承 `QObject` 即 UB
- [src/statisticsbrowser.cpp:64, 96](../../src/statisticsbrowser.cpp#L64) — `listSize == 0` 除零

### Low / Nit

- 整数溢出：[processrunner_unix.cpp:32](../../src/core/processrunner_unix.cpp#L32)、[processrunner_win.cpp:286](../../src/core/processrunner_win.cpp#L286)、[task.cpp:222-230](../../src/core/task.cpp#L222-L230)、[contestant.cpp:158-179](../../src/core/contestant.cpp#L158-L179)
- [src/core/judgingthread.cpp:543-547](../../src/core/judgingthread.cpp#L543-L547) — `score >= fullScore` 不 clamp 到 `fullScore`
- [src/base/LemonTranslator.cpp:45](../../src/base/LemonTranslator.cpp#L45) — `replace(".qm", "")` 替换全部出现，应 `chopped(3)`
- [src/lemon.cpp:46-47](../../src/lemon.cpp#L46-L47) — `Qt::Key_Insert` 同时绑给两个 action

### 已知 TODO

- [src/core/processrunner_unix.cpp:35](../../src/core/processrunner_unix.cpp#L35) — `// TODO: rewrite with cgroup`
- [src/core/taskjudger.cpp:249](../../src/core/taskjudger.cpp#L249) — `// TODO: 需要重构代码来处理含空格路径问题`
- [src/core/judgingcontroller.cpp:72](../../src/core/judgingcontroller.cpp#L72) — `// emit judgeFinished();` 被注释 → stop 后嵌套 QEventLoop 可能不退出

---

## 推荐下一批修复顺序

按 **数据正确性 > 崩溃 > 安全 > 资源** 排：

1. **判题正确性**
   - [taskjudger.cpp:404-405](../../src/core/taskjudger.cpp#L404-L405) 状态污染分数
   - [taskjudger.cpp:416-420](../../src/core/taskjudger.cpp#L416-L420) 跳过测点 UI 丢失
   - [taskjudger.cpp:243-310](../../src/core/taskjudger.cpp#L243-L310) CommunicationExec 编译错误覆盖
   - [processrunner_unix.cpp:30/197](../../src/core/processrunner_unix.cpp#L30) + [processrunner_win.cpp:307](../../src/core/processrunner_win.cpp#L307) 中止运行报 0 分 AC
2. **崩溃 / UB**
   - [lemon.cpp:677](../../src/lemon.cpp#L677) + [opencontestwidget.cpp](../../src/opencontestwidget.cpp#L71) untrusted len
   - [judgingcontroller.cpp:47](../../src/core/judgingcontroller.cpp#L47) use-after-free → `deleteLater()`
   - [taskjudger.cpp:543](../../src/core/taskjudger.cpp#L543) `isJudging` 改 atomic
   - [judgingdialog.cpp:29](../../src/judgingdialog.cpp#L29) 析构顺序
   - [LemonLog.hpp:62](../../src/base/LemonLog.hpp#L62) logger 空指针守卫
3. **安全（XSS / 编码）**
   - [detaildialog.cpp HTML 拼接](../../src/detaildialog.cpp#L139)、[exportutil.cpp:148](../../src/component/exportutil/exportutil.cpp#L148) 的 `Qt::escape` / `toHtmlEscaped`
   - [exportutil.cpp QTextStream 编码](../../src/component/exportutil/exportutil.cpp#L218) 与 [CSV 转义](../../src/component/exportutil/exportutil.cpp#L723)
4. **资源 / 跨平台**
   - watcher 二进制清理、`CreateFileW` 句柄检查、Win/Unix watcher 超时语义统一
