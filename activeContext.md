# 当前上下文（活跃信息）

- 版本与默认：C++17 + Qt6（可切换 Qt5）；默认嵌入翻译与手册，日志输出到本地数据目录按日轮换。
- 当前目标：建立完整中文记忆库，便于后续会话快速同步项目概况、架构与技术约束。
- 近期关注事项（TODO 同步）：配置存储格式统一化（#69）；评测器重写/沙箱化（#64）；WSL 支持（#81）；评分器扩展；UI 方向包含 TUI/CLI（#67）与 QML GUI；macOS Big Sur 崩溃修复（#72）。
- 流程提示：比赛文件建议使用 JSON/CDF；数据目录按题目分子目录；构建产物放 `../build`；评测前确认编译器列表与默认语言/时空限制；清理功能支持备份。
- 已知约束：评测性能取决于本机编译器与多线程配置；交互/通信题主要保证 C++；无远程同步与在线排名。
